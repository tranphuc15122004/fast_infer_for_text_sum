[project]
name = "fast-infer-text-sum"
version = "0.1.0"
description = "Long-context text summarization inference benchmarks"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["externals", "outputs", "checkpoints"]
