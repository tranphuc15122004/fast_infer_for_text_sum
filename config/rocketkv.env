# RocketKV verification config.
# Smoke: verifies the RocketArgs budget math + RocketAttention kernel
# (prefill + decode) on dummy tensors — no model download needed.
TOKEN_BUDGET="512"
SEQ_LEN="2048"
MAX_NEW_TOKENS="64"
HEAD_DIM="128"
NUM_RUNS="2"

SMOKE="1"
FULL="0"

OUTPUT_FILE="outputs/rocketkv_smoke.jsonl"
