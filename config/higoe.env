# HiGOE verification config.
# Smoke: module imports + tiny Contriever retrieval on dummy docs (CPU-ok).
# Full pipeline needs datasets + LLM judge (see externals/HiGOE/README.md).
RETRIEVER_MODEL="facebook/contriever"
NUM_DOCS="3"

SMOKE="1"

OUTPUT_FILE="outputs/higoe_smoke.jsonl"
